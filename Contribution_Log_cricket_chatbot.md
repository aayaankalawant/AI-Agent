# AI Contribution Log

## Contribution 001
**AI Contribution:**
Suggested the basic structure of the `simple_cricket_chatbot()` function with a continuous input loop, similar to the earlier calculator project.

**My Contribution:**
I created the project idea and reviewed the suggested function structure before accepting it.

---

## Contribution 002
**AI Contribution:**
Suggested storing cricketer data as a dictionary of dictionaries, keyed by lowercase name, with fields for country, role, batting style, known-for, and teams.

**My Contribution:**
I checked the data fields and confirmed they covered the information I wanted to display.

---

## Contribution 003
**AI Contribution:**
Suggested a `find_cricketer()` helper that does an exact match first, then falls back to a case-insensitive substring match.

**My Contribution:**
I tested it by typing partial names (e.g. "kohli", "dhoni") and confirmed the right player came up.

---

## Contribution 004
**AI Contribution:**
Suggested handling the case where multiple cricketers match a partial query by listing all matches instead of guessing.

**My Contribution:**
I tested an ambiguous query and verified the suggestion list displayed correctly.

---

## Contribution 005
**AI Contribution:**
Suggested a `list` command to show every cricketer in the database, and a clear "not found" message for unknown names.

**My Contribution:**
I tested both an unknown name and the `list` command to confirm the output was correct.

---

## Contribution 006
**AI Contribution:**
Suggested the `bye` exit condition to stop the chatbot loop.

**My Contribution:**
I modified the goodbye message and tested that the loop exits properly.

---

## Summary

| Contribution | AI Contribution | My Contribution |
|---|---|---|
| 001 | Suggested `simple_cricket_chatbot()` function structure | Planned the project and reviewed the function |
| 002 | Suggested dictionary-of-dictionaries data structure | Checked data fields for completeness |
| 003 | Suggested exact + substring matching logic | Tested with partial names |
| 004 | Suggested handling multiple matches with suggestions | Tested ambiguous queries |
| 005 | Suggested `list` command and not-found message | Tested unknown names and the list command |
| 006 | Suggested `bye` exit condition | Modified goodbye message and tested exit |
